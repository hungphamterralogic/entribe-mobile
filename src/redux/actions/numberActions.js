import { createAction } from "redux-actions";

export const increaseFirstNumByOne = createAction("INCREASE_FIRST_NUMBER_BY_ONE");
export const increaseSecondNumByTwo = createAction("INCREASE_SECOND_NUMBER_BY_TWO");
